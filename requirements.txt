# MedAgent v7 — Python 3.11+
# Install: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0
prompt_toolkit>=3.0.0     # Chat slash-command autocomplete (Tab completion)
rich>=13.0.0              # TUI: spinners, panels, rules, tables

# ── Search ────────────────────────────────────────────────────────────────────
# Brave Search API (set BRAVE_API_KEY in .env) — no extra package, uses requests
# ddgs>=0.5.0  — removed, replaced by Brave Search API

# ── Optional: PDF export (--pdf flag) ────────────────────────────────────────
# pip install markdown weasyprint
# markdown>=3.5.0
# weasyprint>=60.0

# ── Optional: Gemini YouTube full-video analysis ──────────────────────────────
# pip install google-generativeai
# google-generativeai>=0.7.0

# ── Optional: CORE API open-access search ─────────────────────────────────────
# No extra package needed — uses requests. Just set CORE_API_KEY in .env.

# ── Optional: Sci-Hub opt-in (--gray-access flag) ─────────────────────────────
# No extra package needed.

# ── Dev / testing ─────────────────────────────────────────────────────────────
# pytest>=8.0.0
# pytest-cov>=4.0.0
